{
  "name": "@archiyou/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Combined production API — mounts the editor backend (@archiyou/server) and the publish library/execution API (@archiyou/publish) on one Fastify server. Each package remains independently deployable.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@archiyou/publish": "workspace:*",
    "@archiyou/server": "workspace:*",
    "dotenv": "^17.3.1",
    "fastify": "^4.29.1"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
