packages:
  - 'apps/*'
  - 'packages/*'

overrides:
  # dedupe ioredis so bullmq and @archiyou/server share one copy (avoids type clashes)
  ioredis: ^5.11.1

allowBuilds:
  '@vaadin/vaadin-usage-statistics': false
  esbuild: false
  # native module — needs its build script to compile the SQLite binding
  better-sqlite3: true
